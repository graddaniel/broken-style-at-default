import classes from './some-route.module.css';

export default function SomeRoutePage() {
    return (
        <div className={classes.someRouteClass}>Some route page (this should be green)</div>
    );
}