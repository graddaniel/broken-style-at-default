import classes from './some-route.module.css';

export default function SomeRouteDefault() {
    return (
        <div className={classes.someRouteClass}>Some route default (this should be green)</div>
    );
}